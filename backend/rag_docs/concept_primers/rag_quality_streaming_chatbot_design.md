# RAG UX: streaming, citations, token accounting, abstention patterns

## Why streaming (SSE tokens)
Portfolio analysts tolerate latency but perceived responsiveness hinges incremental tokens—Server-Sent Events let partial answers render while generators continue.

## Transparency affordances surfaced to reviewers
**(1)** Collapsible retrieval evidence lists file name snippet category.**(2)** Numeric distance scores assist debugging incorrect chunk relevance.**(3)** Approximate upstream/downstream token bookkeeping educates infra cost instincts.

Grounding prompt clause ("only context below") aligns model behaviour—but enforcement incomplete without verifier modules.

## Abstention & closed ecosystem strategies
**(A)** lexical allow-lists rejecting clearly outside queries.**(B)** LLM echoed refusal templated lines.**(C)** abstain when excerpt absence explicit ("not in KB") preferable hallucination bridging.

Hybrid future: verifier model rating answer faithfulness scoring user trust overlay.
